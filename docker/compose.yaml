services:
  python-app:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    container_name: python-docker-workspace
    gpus: all
    volumes:
      - ..:/workspace
    working_dir: /workspace
    command: sleep infinity
    stdin_open: true
    tty: true
